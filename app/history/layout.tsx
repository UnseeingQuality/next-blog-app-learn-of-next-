import { TheHeader } from '@/components/TheHeader'
import React from 'react'

export default function NavLayot({
    children,
}: {
    children: React.ReactNode;
}) {
    return (
        <TheHeader/>
    )
};